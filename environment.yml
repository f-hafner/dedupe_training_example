name: dedupe-training-example
channels:
  - conda-forge
  - defaults
dependencies:
  - ipykernel
  - ipython
  - nltk
  - numpy
  - pandas
  - pip
  - python ==3.9
  - scipy
  - sqlite
  - pip:
    - dedupe==2.0.14

# NOTE: dedupe 2.0.13 and no constraint on python version does not work. fastcluster dependency...

